---
import Base from '../layouts/Base.astro';
---
<Base
  lang="en"
  pageId="demo"
  title="See CarveTrace verify a real bundle"
  description="Open a real CarveTrace evidence bundle in the independent verifier. Tamper one byte and watch it get rejected."
>
  <h1>[TODO copy: H1 — narrative hook before hand-off]</h1>

  <section>
    <h2>[TODO copy: A real bundle]</h2>
    <p class="todo-copy">
      Bundle generated by a real AI Act compliance workflow (placeholder until Objectif 1 bundle is ready).
      What's inside: signed events, RFC 3161 timestamps, schema registry, verification metadata.
    </p>
  </section>

  <section>
    <h2>[TODO copy: Verify it yourself]</h2>
    <p class="todo-copy">4–5 lines: what the verifier checks, why you do not need to trust us.</p>
    <p>
      <a class="cta" href="https://verify.carvetrace.com" rel="noopener" target="_blank">
        [TODO copy: button — open the verifier]
      </a>
    </p>
  </section>

  <section>
    <h2>[TODO copy: The tamper test]</h2>
    <!--
      GATE: do not promote the tamper sample publicly until
      TD-WASM-VERDICT-LABEL is closed (WASM currently emits
      "ERROR" instead of "FAILED" on byte-flip rejection — reads
      as "verifier crashed", not "proof rejected"). Once the TD
      lands, restore the CTA below.
    -->
    <p class="todo-copy">
      [TODO copy: explain byte-flip → rejection. CTA disabled until TD-WASM-VERDICT-LABEL closed.]
    </p>
  </section>
</Base>
